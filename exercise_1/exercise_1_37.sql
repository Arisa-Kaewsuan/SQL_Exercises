-- เขียนคำสั่ง sql เพื่อแสดงข้อมูลชื่อหนัง(title) ความยาว(length) และความยาวเฉลี่ยของหนัง
-- โดยหาความยาวเฉลี่ยจากเรื่องที่มี film_id ก่อนหน้าเรื่องตัวเองทั้งหมด และเรื่องตัวเอง
-- เช่น ค่าเฉลี่ยของข้อมูลในแถว film_id = 3 จะหาได้จาก(ข้อมูลในแถว film_id_1 + ข้อมูลในแถว film_id_2 + ข้อมูลในแถว film_id_3) / 3

SELECT  title, length, AVG(length) OVER (ORDER BY )
FROM film 